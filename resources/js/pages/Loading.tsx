import LoadingSpinner from "../components/LoadingSpinner";

const Loading: React.FC = () => {
  <h1 className="screen-reader-only">Loading</h1>;
  return <LoadingSpinner />;
};

export default Loading;
